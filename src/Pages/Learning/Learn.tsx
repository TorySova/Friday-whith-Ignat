import React from "react"
import { Grades } from "../Grades/Grades";

export const Learn = () => {
    const cardId = "sdafsdf"

    return (
        <div>
            <Grades cardId={cardId}/>
        </div>
    );
}